import { ClipboardCheck, Clock3, LockKeyhole } from "lucide-react"; import { DashboardHeader } from "@/components/dashboard-header"; import { Sa1Layout } from "@/components/sa1-layout"; import { questionnaires } from "@/lib/sa1-demo-data"; import { QuestionnaireDemoPanel } from "@/components/questionnaire-demo-panel";
export default function Page(){return <><DashboardHeader eyebrow="SA1 · Cuestionarios" title="Cuestionarios" description="Cuestionarios académicos creados por tu profesor/a."/><Sa1Layout><div className="rounded-2xl border border-slate-200 bg-slate-50 p-4 text-sm text-slate-600"><LockKeyhole className="mr-2 inline" size={17}/>Tus respuestas y resultados individuales son privados: solo tú y el profesorado autorizado podéis consultarlos.</div><div className="grid gap-4 md:grid-cols-2">{questionnaires.map(q=><article key={q.id} className="card p-6"><div className="flex items-center justify-between"><ClipboardCheck className="text-[#1e6b4f]"/><span className="rounded-full bg-slate-100 px-3 py-1 text-xs font-bold">{q.status==="completed"?"Completado":"Disponible"}</span></div><h2 className="mt-5 text-lg font-extrabold">{q.title}</h2><p className="mt-2 text-sm leading-6 text-slate-500">{q.description}</p><p className="mt-4 flex items-center gap-2 text-xs text-slate-500"><Clock3 size={14}/>{q.questionCount} preguntas · {q.opensAt}{q.closesAt?` — ${q.closesAt}`:""}</p><button className="mt-5 rounded-xl bg-[#1e6b4f] px-4 py-2.5 text-sm font-bold text-white">{q.status==="completed"?"Revisar mis respuestas":"Responder"}</button></article>)}</div><QuestionnaireDemoPanel/><p className="text-xs leading-5 text-slate-500">Los datos demo no contienen ningún test psicológico real. El motor admite texto, selección única/múltiple y escalas 1–5, 1–7 y 1–10.</p></Sa1Layout></>}